//{ photo, barCodeImg, name, direction, state, stateShort, CP }

export type ILeftData = {
    photo: string, 
    barCodeImg: string,
    name: string,
    direction: string,
    state: string,
    stateShort: string,
    CP: string
}